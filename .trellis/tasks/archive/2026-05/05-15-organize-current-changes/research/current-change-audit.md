# Current Change Audit

Audit date: 2026-05-15 Asia/Shanghai

## Inputs Reviewed

- `git status --short` shows tracked changes across Trellis template files, release workflow/scripts, Android Gradle files, and frontend Trellis specs.
- `trellis update --dry-run` reports only one template conflict: `.trellis/.gitignore`; the local file is not modified relative to Git HEAD and its extra ignore rules are intentional local runtime hygiene.
- Ignored Trellis update outputs are `.trellis/.gitignore.new` and `.trellis/.backup-*`; they are not commit candidates.
- Recent history contains `048f4364 chore: upgrade Flutter toolchain to 3.41.9`, and archived task evidence records successful dependency resolution and Android debug build on Flutter `3.41.9`.
- Project frontend specs define Flutter `3.41.9`, Dart `3.11.5`, Java `17`, and `lib/src/main.dart` as the build entrypoint.

## Classification

### Trellis template / workflow update

Files:

- `.trellis/.version`
- `.trellis/workflow.md`
- `.agents/skills/trellis-meta/references/local-architecture/task-system.md`

Conclusion: keep and commit together. These files move local Trellis metadata to `0.6.0-beta.17` and document parent/child task tree behavior. They are logically separate from app build changes.

### Release and dependency policy documentation

Files:

- `.trellis/spec/frontend/index.md`
- `.trellis/spec/frontend/quality-guidelines.md`
- `.trellis/spec/frontend/release-packaging.md`

Conclusion: keep and commit as documentation/spec. The new release packaging spec captures the source of truth for CI artifacts, platform entrypoints, signing expectations, disabled AppImage status, and Gradle repository synchronization. The quality guideline update links release policy and records dependency-resolution ordering.

### Release workflow and helper scripts

Files:

- `.github/workflows/build_publish.yml`
- `.gitignore`
- `linux.sh`
- `thin-payload.sh`
- `windows.sh`

Conclusion: keep and commit together. These changes align CI and helper scripts with Flutter `3.41.9`, stable channel setup, correct iOS target condition, correct CocoaPods cache key, Windows release output path, and safer shell handling. `.gitignore` excludes `/android/build/`, which is generated by Gradle/Flutter validation.

Follow-up action taken during audit: touched shell scripts were made `shellcheck` clean by adding shell directives, quoting paths, and using `dest` variables.

### Android Gradle repository resolution

Files:

- `android/build.gradle`
- `android/settings.gradle`

Conclusion: keep and commit separately. The repository order is intentionally duplicated between plugin management and project repositories, with Flutter Maven repositories content-filtered to `io.flutter`, official `google()` before NJU Maven, and Aliyun fallbacks. This deserves a separate review surface because it affects all Android dependency resolution.

## Validation Evidence So Far

- `git diff --check`: passed.
- `flutter pub get` with `/home/ubuntu/upgrade-sandbox/jhentai/flutter/bin/flutter` `3.41.9`: passed.
- `./gradlew help --dry-run` from `android/`: passed, with existing warnings about plugin compileSdk overrides and Gradle deprecations.
- `shellcheck linux.sh thin-payload.sh windows.sh`: passed after script cleanup.
- `bash -n` for `linux.sh`, `thin-payload.sh`, and `windows.sh`: passed.
- `flutter build apk --debug -t lib/src/main.dart` with Flutter `3.41.9`, isolated `PUB_CACHE`, and isolated `GRADLE_USER_HOME`: passed.
  - Output: `build/app/outputs/flutter-apk/app-debug.apk`
  - Size: 164.8M
  - SHA1: `25bcff28fd814942fe65e42dc7b79e79abf3dcbd`
- `actionlint` is not installed; `ruby` is not available for quick YAML parsing in this environment.

## Commit Plan

1. `1ad20b7e chore(trellis): update workflow templates`
2. `f29800c0 docs: record release packaging policy`
3. `0bc8fedc ci: align release packaging scripts`
4. `db3d82f4 build(android): add Maven fallback repositories`
5. `chore(task): archive organize-current-changes`

The task archive commit should be last so the task record captures final commit hashes and validation outcomes.
